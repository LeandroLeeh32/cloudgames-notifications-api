apiVersion: v1
kind: ConfigMap
metadata:
  name: notifications-config
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  RABBITMQ_HOST: "rabbitmq-service"
  RABBITMQ_VIRTUAL_HOST: "/"
  USER_CREATED_QUEUE: "user-created-queue"
  PURCHASE_CREATED_QUEUE: "purchase-created-queue"