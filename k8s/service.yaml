apiVersion: v1
kind: Service
metadata:
  name: notifications-functions-service
spec:
  selector:
    app: notifications-functions
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30086
  type: NodePort
